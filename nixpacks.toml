[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd thinkmate-next && npm install"]

[phases.build]
cmds = ["cd thinkmate-next && npm run build"]

[start]
cmd = "cd thinkmate-next && npm start"
